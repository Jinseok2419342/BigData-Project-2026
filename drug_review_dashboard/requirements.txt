# Hand-curated dependencies for the Drug Review ADE dashboard.
# NOTE: Streamlit Cloud runs Python 3.14 — packages must have Python 3.14-compatible
# pre-built wheels. Use >= bounds instead of ~= to avoid stale local pip-index issues.

# --- Streamlit dashboard ---
streamlit>=1.45,<2.0

# --- Data processing ---
pandas>=2.2,<3.0
numpy>=2.0,<3.0            # 2.x required: 1.x has no Python 3.14 wheels

# --- Machine learning ---
scikit-learn>=1.5,<2.0

# --- Visualization ---
plotly>=5.24,<7.0
matplotlib>=3.9,<4.0
seaborn>=0.13,<1.0

# --- Dataset loading ---
kagglehub>=0.3,<2.0

# --- LLM integration ---
ollama>=0.3,<1.0
openai>=2.0,<3.0           # code uses the 2.x SDK API

# --- Environment variable management ---
python-dotenv>=1.0,<2.0

# --- Image upload preview ---
pillow>=11.0,<13.0         # 11+ required: 10.x has no Python 3.14 wheels

# --- R&D notebook ---
jupyterlab>=4.2,<5.0

# --- Testing ---
pytest>=8.3,<10.0
