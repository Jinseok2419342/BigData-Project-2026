# Hand-curated dependencies for the Drug Review ADE dashboard.
# Only packages directly imported by app.py / src/ / pages/ / eda_report.py /
# notebooks/ / tests/ are listed; transitive deps are resolved by pip.
# Compatible-release pins (~=) keep installs reproducible without locking
# to environment-specific builds.

# --- Streamlit dashboard (app.py, pages/, src/data_cache.py) ---
streamlit~=1.45

# --- Data processing (src/data_loader.py, src/features.py) ---
pandas~=2.2
numpy~=1.26

# --- Machine learning (src/modeling.py) ---
scikit-learn~=1.5

# --- Visualization (pages/ use plotly; eda_report.py + notebook use matplotlib/seaborn) ---
plotly~=5.24
matplotlib~=3.9
seaborn~=0.13

# --- Dataset loading via KaggleHub (src/data_loader.py) ---
kagglehub~=0.3

# --- LLM integration: local Ollama first, OpenAI backup (src/llm_helper.py) ---
ollama~=0.3
openai~=1.59

# --- Environment variable management: .env loading (src/llm_helper.py) ---
python-dotenv~=1.0

# --- Image upload preview on the model service page (pages/3_Model_Service.py) ---
pillow~=10.4

# --- R&D notebook (notebooks/EDA_and_Preprocessing_Analysis.ipynb) ---
jupyterlab~=4.2

# --- Testing (tests/test_llm_routing.py) ---
pytest~=8.3
